import { SC_ContainerHome } from "./styles";

const ContainerHome = ({children}) => {
    return (
    <SC_ContainerHome>
        {children}
    </SC_ContainerHome>
        );
}

export default ContainerHome;