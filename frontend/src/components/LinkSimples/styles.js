import styled from 'styled-components';

export const SC_LinkSimples = styled.a`
        font-family: 'Raleway', serif;
        color: var(--cor-principal);
        & strong {
            color: black;
            font-weight: normal;
        }
    `;